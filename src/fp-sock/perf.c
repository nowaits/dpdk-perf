#include "envs.h"

#if 0
    uint64_t c1 = odp_cpu_cycles();
    c2 = odp_cpu_cycles();

    tot_cycles += odp_cpu_cycles_diff(c2, c1);
    tot_cycles * 1E6 / odp_cpu_hz()
    odp_time_to_ns
    odp_time_global
#endif